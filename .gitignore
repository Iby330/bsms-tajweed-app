.env
.env.local
*.pyc
__pycache__/
.venv/
venv/
node_modules/
.DS_Store
.claude/skill-research/
.claude/settings.local.json
dist/
build/
.next/
*.log

# ── privacy: real-student data never leaves this machine ──
brainstorms/
Tajweed Master Spreadsheet 2025_26/
Tawjeed HW Master Guide .md
# ── local tooling / unrelated workspaces ──
.tooling/
.claude/
Agentic Workflow Transition Workspace/
*.code-workspace
supabase/.temp/
execution/node_modules
.superpowers/

# browser debug artifacts from playwright-mcp sessions
.playwright-mcp/

# vendored agent skills — the `skills` CLI writes a second copy here alongside
# .claude/skills (which is already ignored above). 184K of third-party markdown
# that would otherwise land in a public repo.
.agents/

# browser artifacts from playwright-cli sessions (sibling of .playwright-mcp)
.playwright-cli/

# real people's addresses and rosters — this repo is public
execution/*.local.json
execution/roster*.json
